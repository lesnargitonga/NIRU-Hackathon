# Core
numpy>=1.24,<2.0
opencv-python>=4.8
pandas>=2.1,<2.4
tqdm>=4.66
PyYAML>=6.0
matplotlib>=3.7
scikit-image>=0.22
albumentations>=1.4

# Deep Learning (CPU by default; install CUDA wheels separately if needed)
# Leave torch/torchvision broad but compatible with known good combos (e.g., torch 2.9.0+cpu, torchvision 0.24.0+cpu)
torch>=2.2
torchvision>=0.17,<0.25
# TensorFlow (if present) prefers tensorboard < 2.16; this avoids resolver conflicts on Windows
tensorboard>=2.14,<2.16
einops>=0.7

# RL and Simulation
# SB3 2.7.0 works well with Gymnasium ~1.2 on Windows
gymnasium>=1.2,<1.3
stable-baselines3>=2.7,<2.8
airsim>=1.8.1
mavsdk>=1.6

# Optional utils
typing-extensions>=4.9